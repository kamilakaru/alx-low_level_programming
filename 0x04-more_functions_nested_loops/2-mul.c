#include "main.h"

/**
 * mul - function that multiples two interger
 * @a: first input
 * @b: second input
 * Return: multiplication result pf the 2 integers
 */

int mul(int a, int b)
{
	return (a * b);
}
